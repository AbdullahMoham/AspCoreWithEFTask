@page
@model UserPermissionLicensesTask.Pages.permission.showpermissionsModel
@{
}


<style>
    table {
        font-family: arial, sans-serif;
        border-collapse: collapse;
        width: 100%;
    }

    td, th {
        border: 1px solid #dddddd;
        text-align: left;
        padding: 8px;
    }

    tr:nth-child(even) {
        background-color: #dddddd;
    }
</style>


<h2>
    <a class="btn btn-info" asp-page="Create">Create New Permission</a>
</h2>

<table>
    <tr>
        <th>Permission_ID</th>
        <th>Permission_Title</th>
        @*<th></th>*@
    </tr>

    @foreach (var item in Model.AllPer)
    {
        <tr>
            <td>@item.ID</td>
            <td>@item.Permission_Title</td>
            @*<td>
                    <a asp-page="Details" asp-route-id="@item.ID">Details</a>
                    <a asp-page="Edit" asp-route-id="@item.ID"> | Edit | </a>
                    <a asp-page="Delete" asp-route-id="@item.ID">Delete</a>
                </td>*@
        </tr>
    }

</table>

